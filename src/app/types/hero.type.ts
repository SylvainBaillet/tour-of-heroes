export class Hero {
  // on crée une simple class, sans y appliquer de component, avec juste un id et un string, cette class est un nouveau type que je vais pouvoir utiliser dans un composant
  id:  number;
  name: string;
}
